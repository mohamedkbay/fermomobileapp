
  # Customer Dashboard Overview

  This is a code bundle for Customer Dashboard Overview. The original project is available at https://www.figma.com/design/96DyPDtpvi6rgppCESLaZV/Customer-Dashboard-Overview.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  